\documentclass[a4paper, 11pt]{article}

% Encoding en fonts (xelatex)
\usepackage{fontspec}

% Pagina-indeling — vaste marges, altijd A4
\usepackage[a4paper, top=2.2cm, bottom=2.2cm, left=2.5cm, right=2.5cm, nohead, nofoot]{geometry}

% Links
\usepackage[hidelinks]{hyperref}

% Typografie
\usepackage{parskip}
\usepackage{microtype}

% Geen paginanummers
\pagestyle{empty}

% PDF metadata (titel = functie + bedrijf)
\hypersetup{
  pdftitle={Sollicitatie -- $functie$ -- $bedrijf$},
  pdfauthor={$naam$}
}

% Instellingen
\setlength{\parskip}{0.65em}
\setlength{\parindent}{0pt}

% Voorkom overflow bij lange woorden
\sloppy

% Pandoc lijsten fix
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}

% ================================================================
% AFZENDER (rechts bovenaan)
% ================================================================
\begin{flushright}
\textbf{$naam$}\\
$if(adres)$$adres$\\$endif$%
$if(postcode)$$postcode$ $endif$$stad$$if(land)$, $land$$endif$\\
\vspace{0.2em}
$if(telefoon)$$telefoon$\\$endif$%
\href{mailto:$email$}{$email$}\\
$if(website_url)$\href{https://$website_url$}{$website_url$}$endif$
\end{flushright}

\vspace{0.4cm}

% ================================================================
% DATUM
% ================================================================
\begin{flushright}
$if(stad)$$stad$$endif$, $datum$
\end{flushright}

\vspace{0.7cm}

% ================================================================
% GEADRESSEERDE
% ================================================================
\textbf{$bedrijf$}\\
$if(afdeling)$$afdeling$\\$endif$%
$if(ter_attentie)$T.a.v. $ter_attentie$\\$endif$%
$if(bedrijf_adres)$$bedrijf_adres$\\$endif$%
$if(bedrijf_postcode)$$bedrijf_postcode$ $endif$$if(bedrijf_stad)$$bedrijf_stad$$endif$

\vspace{0.7cm}

% ================================================================
% ONDERWERP
% ================================================================
\textbf{Betreft: Sollicitatie $functie$$if(referentie)$ \textnormal{(ref: $referentie$)}$endif$}

\vspace{0.7cm}

% ================================================================
% BRIEF INHOUD
% ================================================================
$aanhef$

\vspace{0.3em}

$body$

\vspace{1.2cm}

Met vriendelijke groet,

\vspace{2.0cm}

\textbf{$naam$}

\end{document}
